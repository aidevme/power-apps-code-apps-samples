import { makeStyles } from '@fluentui/react-components'

/** Styles for the {@link LanguageSelector} component. */
export const useLanguageSelectorStyles = makeStyles({
  flag: {
    width: '20px',
    height: 'auto',
    verticalAlign: 'middle',
    marginRight: '6px',
  },
})
