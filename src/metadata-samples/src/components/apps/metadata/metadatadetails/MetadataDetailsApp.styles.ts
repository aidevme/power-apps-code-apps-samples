// AI-CONTEXT: Fluent UI makeStyles hook for the MetadataDetailsApp component.
// AI-CONSTRAINT: Never barrel-export this hook; import it only inside MetadataDetailsApp.tsx.
// AI-PATTERN: One makeStyles call per component style file.

import { makeStyles } from '@fluentui/react-components'

/**
 * Fluent UI makeStyles hook for the {@link MetadataDetailsApp} component.
 */
export const useMetadataDetailsAppStyles = makeStyles({
})
